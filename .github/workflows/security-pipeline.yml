name: Comprehensive Security Scanning Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:
    inputs:
      runner:
        description: 'Choose runner type'
        required: true
        default: 'ubuntu-latest'
        type: choice
        options:
          - ubuntu-latest
          - self-hosted

env:
  REGISTRY: ghcr.io
  MOBSF_DOCKER_IMAGE: mobsf:latest

jobs:
  # SAST: CodeQL analysis for code vulnerabilities
  codeql:
    name: CodeQL SAST Analysis
    runs-on: [self-hosted, windows]
    strategy:
      matrix:
        language: [ 'cpp', 'csharp', 'go', 'java', 'javascript', 'python' ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go (for CodeQL Go)
        if: matrix.language == 'go'
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Set up Java (for CodeQL Java)
        if: matrix.language == 'java'
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@v2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v2

      - name: Upload CodeQL SARIF
        uses: actions/upload-artifact@v4
        with:
          name: codeql-results-${{ matrix.language }}
          path: results.sarif

  # Secret scanning with Trufflehog
  secret-scan:
    name: Secret Detection (Trufflehog)
    runs-on: [self-hosted, windows]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Run Trufflehog
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --json --fail

      - name: Upload secret scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trufflehog-results
          path: trufflehog-results.json

  # Dependency auditing with Snyk
  dependency-scan:
    name: Dependency Audit (Snyk)
    runs-on: [self-hosted, windows]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Snyk scan
        uses: snyk/actions/setup@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Snyk test for vulnerabilities
        run: |
          snyk auth $SNYK_TOKEN
          snyk test --severity-threshold=high --json > snyk-results.json || true

      - name: Upload Snyk results
        uses: actions/upload-artifact@v4
        with:
          name: snyk-results
          path: snyk-results.json

  # Infrastructure as Code scanning with Checkov
  iac-scan:
    name: IaC Scanning (Checkov)
    runs-on: [self-hosted, windows]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Checkov scan
        uses: bridgecrewio/checkov-action@master
        with:
          directory: terraform/
          framework: terraform
          quiet: false
          soft_fail: false
          output_format: json
          output_file_path: checkov-results.json

      - name: Upload Checkov results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkov-results
          path: checkov-results.json

  # Custom data security validation with Judge Script
  security-judge:
    name: Custom Data Security Validation
    runs-on: [self-hosted, windows]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PyYAML jsonschema requests

      - name: Run security judge script
        run: python scripts/security_judge.py
        env:
          GITHUB_REPO: ${{ github.repository }}
          COMMIT_SHA: ${{ github.sha }}

      - name: Upload judge script results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: judge-script-results
          path: judge-results.json

  # MobSF Mobile Security Analysis (on-demand EC2)
  mobsf-scan:
    name: Mobile Security Framework (MobSF)
    runs-on: [self-hosted, linux]
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Start EC2 instance
        id: start-instance
        run: |
          echo "Starting EC2 instance..."
          aws ec2 start-instances --instance-ids ${{ secrets.MOBSF_INSTANCE_ID }}
          
          echo "Waiting for instance to be running..."
          aws ec2 wait instance-running --instance-ids ${{ secrets.MOBSF_INSTANCE_ID }}
          
          # Get public IP
          INSTANCE_IP=$(aws ec2 describe-instances \
            --instance-ids ${{ secrets.MOBSF_INSTANCE_ID }} \
            --query 'Reservations[0].Instances[0].PublicIpAddress' \
            --output text)
          
          echo "instance_ip=$INSTANCE_IP" >> $GITHUB_OUTPUT
          echo "MobSF instance started at $INSTANCE_IP"
          
          # Wait for MobSF to be ready
          sleep 30

      - name: Find APK/IPA files
        id: find-apps
        run: |
          find . -type f \( -name "*.apk" -o -name "*.ipa" \) > apps.txt
          if [ -s apps.txt ]; then echo "found=true" >> $GITHUB_OUTPUT; else echo "found=false" >> $GITHUB_OUTPUT; fi

      - name: Upload to MobSF
        if: steps.find-apps.outputs.found == 'true'
        run: |
          MOBSF_URL="http://${{ steps.start-instance.outputs.instance_ip }}:8000"
          while IFS= read -r app; do
            echo "Uploading $app to MobSF..."
            curl -F "file=@$app" \
              -H "Authorization: ${{ secrets.MOBSF_API_KEY }}" \
              $MOBSF_URL/api/v1/upload
          done < apps.txt

      - name: Stop EC2 instance
        if: always()
        run: |
          echo "Stopping EC2 instance..."
          aws ec2 stop-instances --instance-ids ${{ secrets.MOBSF_INSTANCE_ID }}

  # DAST: OWASP ZAP API Scanning
  api-dast:
    name: API Security Testing (OWASP ZAP)
    runs-on: [self-hosted, linux]
    services:
      zap:
        image: owasp/zap2docker-stable:latest
        options: >-
          --health-cmd "curl -f http://localhost:8080 || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 8080:8080
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Extract API endpoints
        id: extract-api
        run: |
          if [ -f "api-endpoints.txt" ]; then
            echo "api_url=$(head -1 api-endpoints.txt)" >> $GITHUB_OUTPUT
          fi

      - name: Start API server (if applicable)
        run: |
          if [ -f "docker-compose.yml" ]; then
            docker-compose up -d
            sleep 10
          fi

      - name: Run ZAP API scan
        run: |
          docker run --network host \
            -v $(pwd):/zap/wrk:rw \
            owasp/zap2docker-stable zap-api-scan.py \
            -t http://localhost:8000/api/v1/openapi.json \
            -f openapi \
            -r zap-api-report.html \
            -J zap-results.json

      - name: Upload ZAP results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-api-results
          path: |
            zap-api-report.html
            zap-results.json

  # Aggregate and report all security findings
  security-report:
    name: Security Findings Aggregation
    needs: [ codeql, secret-scan, dependency-scan, iac-scan, security-judge, mobsf-scan, api-dast ]
    runs-on: [self-hosted, windows]
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Download all scan results
        uses: actions/download-artifact@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Aggregate findings
        run: python scripts/aggregate_findings.py

      - name: Comment PR with findings
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const findings = JSON.parse(fs.readFileSync('aggregated-findings.json', 'utf8'));
            const comment = `## 🔒 Security Scan Results\n\n${findings.summary}`;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });

      - name: Upload aggregated report
        uses: actions/upload-artifact@v4
        with:
          name: security-report
          path: |
            aggregated-findings.json
            security-summary.txt

      - name: Fail if critical findings
        run: |
          if [ "$(python scripts/check_severity.py critical)" -gt 0 ]; then
            echo "Critical security findings detected!"
            exit 1
          fi
